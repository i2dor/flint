bin/
obj/
publish/
*.user
.DS_Store
.idea/
.vs/
packages/

# Agent worktrees: transient, and each is its own git repo.
.claude/worktrees/

# Local-only deploy/test notes (never commit).
.local/
TESTING.local.md
*.local.md
